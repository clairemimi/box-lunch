{% extends 'base.html.twig' %}

{% block title %}DeliveryMethod{% endblock %}

{% block body %}
    <h1>DeliveryMethod</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ delivery_method.id }}</td>
            </tr>
            <tr>
                <th>Method_name</th>
                <td>{{ delivery_method.methodName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_delivery_method_index') }}">back to list</a>

    <a href="{{ path('app_delivery_method_edit', {'id': delivery_method.id}) }}">edit</a>

    {{ include('delivery_method/_delete_form.html.twig') }}
{% endblock %}
