<form method="post" action="{{ path('app_delivery_method_delete', {'id': delivery_method.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delivery_method.id) }}">
    <button class="btn">Delete</button>
</form>
